import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div className="flex h-screen flex-col items-center justify-center">
      <Button className="bg-black px-5 text-cyan-500">Bootcamp</Button>
    </div>
  );
};

export default Home;
